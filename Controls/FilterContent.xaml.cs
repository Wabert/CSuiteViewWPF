using System.Windows.Controls;

namespace CSuiteViewWPF.Controls
{
    /// <summary>
    /// Reusable filter content control that displays a checklist of filter options.
    /// This replaces the duplicate filter popup markup in the DataGrid columns.
    /// </summary>
    public partial class FilterContent : UserControl
    {
        public FilterContent()
        {
            InitializeComponent();
        }
    }
}
