<UserControl x:Class="CSuiteViewWPF.TreeViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:CSuiteViewWPF.Models"
             Height="Auto" Width="Auto">

    <UserControl.Resources>
        <!-- Navigation tree item template (copied from MainWindow)
             Made implicit by DataType so it applies to all levels of the tree -->
    <HierarchicalDataTemplate DataType="{x:Type models:TreeViewControlNode}" ItemsSource="{Binding Children}">
            <TextBlock Foreground="#2C5AA0">
                <Run Text="{Binding Name}"/>
                <Run Text="  "/>
                <Run Text="{Binding Type}" FontStyle="Italic" Foreground="#CC2C5AA0"/>
            </TextBlock>
        </HierarchicalDataTemplate>

        <!-- Flat TreeView style (copied) -->
        <Style x:Key="FlatTreeViewStyle" TargetType="TreeView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeView">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer Focusable="False"
                                          HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

       <!-- Navigation TreeViewItem style (copied and adapted)
           Made implicit by removing x:Key so it applies to all TreeViewItem elements -->
       <Style TargetType="TreeViewItem">
          <!-- Suppress automatic RequestBringIntoView raised by built-in selection logic -->
          <EventSetter Event="RequestBringIntoView" Handler="TreeViewItem_RequestBringIntoView"/>
            <Setter Property="Foreground" Value="#2C5AA0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>

                            <Border x:Name="HeaderBorder"
                                    Background="Transparent"
                                    CornerRadius="6"
                                    Padding="2,0,5,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <ToggleButton x:Name="Expander"
                                                  Grid.Column="0"
                                                  Width="14"
                                                  Height="14"
                                                  Margin="0,0,5,0"
                                                  VerticalAlignment="Center"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                  ClickMode="Press"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Focusable="False">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border x:Name="ToggleBox"
                                                        Width="14"
                                                        Height="14"
                                                        Background="#60A0FF"
                                                        BorderBrush="#2C5AA0"
                                                        BorderThickness="1"
                                                        CornerRadius="6"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center">
                                                    <Grid>
                                                        <Rectangle Width="8" Height="2" Fill="#2C5AA0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        <Rectangle x:Name="VerticalBar" Width="2" Height="8" Fill="#2C5AA0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Grid>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="VerticalBar" Property="Visibility" Value="Collapsed"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ToggleBox" Property="Background" Value="#75B4FF"/>
                                                        <Setter TargetName="ToggleBox" Property="BorderBrush" Value="#23467D"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>

                                    <ContentPresenter Grid.Column="1"
                                                      ContentSource="Header"
                                                      RecognizesAccessKey="True"
                                                      VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                            <ItemsPresenter x:Name="ItemsHost"
                                            Grid.Row="1"
                                            Margin="16,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="False">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsMouseDirectlyOver" Value="True">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="#A7C9FF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <!-- softer gold for selected item -->
                                <Setter TargetName="HeaderBorder" Property="Background" Value="#FFC86A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="Transparent" Padding="4">
        <!-- implicit HierarchicalDataTemplate via DataType will be applied to all TreeViewControlNode instances -->
        <TreeView x:Name="NavTree" AllowDrop="True" Drop="NavTree_Drop" PreviewDragOver="NavTree_PreviewDragOver"
                  Background="Transparent" BorderThickness="0" Padding="2" VerticalAlignment="Stretch"
                  Style="{StaticResource FlatTreeViewStyle}"/>
    </Border>
</UserControl>
