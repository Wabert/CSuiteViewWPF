<UserControl x:Class="CSuiteViewWPF.ListViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:CSuiteViewWPF.Models"
             Height="Auto" Width="Auto">

    <!-- Outer wrapper: make transparent so only the gold border is visible -->
        <Border Padding="4" CornerRadius="6" MaxWidth="440" Background="Transparent" BorderThickness="0">
        <!-- gold etched border: use a gradient stroke effect by layering borders -->
            <Border BorderBrush="{StaticResource GoldBrush}" BorderThickness="2" CornerRadius="5" Background="Transparent">
            <Grid>
                <!-- inner highlight to simulate etch; place the ListView inside so the background fills under content -->
                <Border Margin="3" CornerRadius="4" Padding="8">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#BFE0FF" Offset="0.0" />
                            <GradientStop Color="#A7C9FF" Offset="0.6" />
                        </LinearGradientBrush>
                    </Border.Background>

                    <ListView x:Name="ItemsHost"
                              Margin="0"
                              ItemsSource="{Binding}"
                              SelectionMode="Single"
                              Background="Transparent"
                              BorderThickness="0"
                              VirtualizingStackPanel.IsVirtualizing="True"
                              VirtualizingStackPanel.VirtualizationMode="Recycling"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              KeyboardNavigation.TabNavigation="Local"
                              >

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="4,2" />
                        <Setter Property="Margin" Value="0,1" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Setter Property="MinHeight" Value="28" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" SnapsToDevicePixels="true" CornerRadius="4">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="true">
                                            <Setter TargetName="Bd" Property="Background" Value="{Binding Source={StaticResource GoldBrush}, Converter={StaticResource BrushToSelectionBackgroundConverter}, FallbackValue=#33FFD700}" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="true">
                                            <Setter TargetName="Bd" Property="Background" Value="#22FFFFFF" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:KeyValueItem}">
                        <Grid ToolTipService.IsEnabled="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Key}" Foreground="Black" FontWeight="SemiBold" Grid.Column="0" VerticalAlignment="Center" />
                            <Border Grid.Column="1" Background="Transparent" Padding="2">
                                <TextBlock Text="{Binding Value}" x:Name="ValueText" TextWrapping="Wrap" Foreground="Black" FontSize="12" />
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
                    </Border>
                </Grid>
        </Border>
    </Border>
</UserControl>
