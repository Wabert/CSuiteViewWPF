<Application x:Class="CSuiteViewWPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CSuiteViewWPF"
             xmlns:converters="clr-namespace:CSuiteViewWPF.Converters"
             >
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- MahApps.Metro resource dictionaries -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Blue.xaml" />
                
                <!-- Our custom theme -->
                <ResourceDictionary Source="Themes/RoyalClassicTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Converters -->
            <converters:BrushToSelectionBackgroundConverter x:Key="BrushToSelectionBackgroundConverter" />

            <!-- Default tokens so styles can resolve at app scope -->
            <SolidColorBrush x:Key="GoldBrush" Color="#FFD700" />
            <SolidColorBrush x:Key="DarkBlue" Color="#27408B" />

            <!-- Gold bubbled button style -->
            <Style x:Key="GoldBubbledButton" TargetType="Button">
                <Setter Property="Foreground" Value="#18325F" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <!-- Outer rounded shape with metallic gradient -->
                                <Border x:Name="Outer" CornerRadius="8" BorderThickness="1" BorderBrush="#8C6B00">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#FFF6E0" Offset="0.0"/>
                                            <GradientStop Color="#FFD24D" Offset="0.5"/>
                                            <GradientStop Color="#FFB800" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>

                                <!-- Subtle inner highlight for 3D effect -->
                                <Border CornerRadius="7" Margin="2" IsHitTestVisible="False">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#80FFFFFF" Offset="0.0"/>
                                            <GradientStop Color="#00FFFFFF" Offset="0.6"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>

                                <!-- Content presenter -->
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />

                                <!-- Pressed overlay -->
                                <Border x:Name="PressedOverlay" CornerRadius="8" Background="#40000000" Opacity="0" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Outer" Property="BorderBrush" Value="#A67C00" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="PressedOverlay" Property="Opacity" Value="0.35" />
                                    <Setter TargetName="Outer" Property="RenderTransform">
                                        <Setter.Value>
                                            <TranslateTransform Y="1" />
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Dark blue background with gold text, graded look -->
            <Style x:Key="DarkBlueGoldButton" TargetType="Button">
                <Setter Property="Foreground" Value="{StaticResource GoldBrush}" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Border x:Name="Root" CornerRadius="6" BorderThickness="1" BorderBrush="#18325F">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#27408B" Offset="0.0"/>
                                            <GradientStop Color="#1F3A78" Offset="0.5"/>
                                            <GradientStop Color="#18325F" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>

                                <!-- Slight inner sheen -->
                                <Border CornerRadius="5" Margin="2" IsHitTestVisible="False">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#40FFFFFF" Offset="0.0"/>
                                            <GradientStop Color="#00FFFFFF" Offset="0.5"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>

                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />

                                <Border x:Name="PressedOverlay" CornerRadius="6" Background="#20000000" Opacity="0" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Root" Property="BorderBrush" Value="#2C5AA0" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="PressedOverlay" Property="Opacity" Value="0.25" />
                                    <Setter TargetName="Root" Property="RenderTransform">
                                        <Setter.Value>
                                            <TranslateTransform Y="1" />
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Themed TextBox matching app theme -->
            <Style x:Key="ThemedTextBox" TargetType="TextBox">
                <Setter Property="Foreground" Value="#18325F" />
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#FFFFFFFF" Offset="0.0" />
                            <GradientStop Color="#F0F6FF" Offset="0.5" />
                            <GradientStop Color="#E6F0FF" Offset="1.0" />
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <Setter Property="Padding" Value="6,4" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="{StaticResource DarkBlue}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Grid>
                                <!-- Outer border with subtle bevel -->
                                <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                                    <Grid>
                                        <!-- content host -->
                                        <ScrollViewer x:Name="PART_ContentHost" Margin="4,2,4,2" />

                                        <!-- inner sheen to give a soft 3D feel -->
                                        <Rectangle IsHitTestVisible="False" RadiusX="6" RadiusY="6">
                                            <Rectangle.Fill>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#60FFFFFF" Offset="0.0" />
                                                    <GradientStop Color="#00FFFFFF" Offset="0.5" />
                                                </LinearGradientBrush>
                                            </Rectangle.Fill>
                                        </Rectangle>
                                    </Grid>
                                </Border>

                                <!-- Focus glow: gold halo using DropShadowEffect on a transparent border -->
                                <Border x:Name="FocusGlow" CornerRadius="6" Background="Transparent" Opacity="0">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#FFD16A" BlurRadius="14" ShadowDepth="0" Opacity="0.9" />
                                    </Border.Effect>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="FocusGlow" Property="Opacity" Value="1" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.6" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
