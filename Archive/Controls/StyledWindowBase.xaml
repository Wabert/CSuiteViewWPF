<UserControl x:Class="CSuiteViewWPF.Controls.StyledWindowBase"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CSuiteViewWPF.Controls">

    <!-- Archived: legacy UserControl-based styled window. Replaced by Views.StyleWindow + WindowChrome template. -->
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/BrushResources.xaml"/>
                <ResourceDictionary Source="/Resources/ButtonStyles.xaml"/>
                <ResourceDictionary Source="/Resources/ControlTemplates.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="{DynamicResource GoldBrush}" 
            BorderBrush="{DynamicResource GoldBrush}"
            BorderThickness="2"
            CornerRadius="2"
            Padding="2">
        <Border Background="{DynamicResource DarkBlueBrush}"
                CornerRadius="1">
            <DockPanel>
                <Border x:Name="HeaderBorder"
                        DockPanel.Dock="Top"
                        Background="{DynamicResource DarkBlueBrush}"
                        Height="48">
                    <DockPanel>
                        <TextBlock x:Name="HeaderTitleTextBlock"
                                  Text="Styled Window"
                                  FontSize="20"
                                  FontWeight="Bold"
                                  Foreground="{DynamicResource GoldBrush}"
                                  VerticalAlignment="Center"
                                  Margin="20,0,0,0"
                                  DockPanel.Dock="Left"/>
                        <Button x:Name="CloseButton"
                                Style="{DynamicResource GoldCircleCloseButton}"
                                DockPanel.Dock="Right"
                                Margin="0,0,10,0"/>
                    </DockPanel>
                </Border>
                <Rectangle DockPanel.Dock="Top" Height="0.5" Fill="{DynamicResource GoldBrush}"/>
                <Border x:Name="FooterBar" DockPanel.Dock="Bottom" Background="{DynamicResource DarkBlueBrush}" Height="36">
                    <TextBlock Text="Footer" Foreground="{DynamicResource GoldBrush}" VerticalAlignment="Center" Margin="12,0"/>
                </Border>
                <Rectangle x:Name="FooterLine" DockPanel.Dock="Bottom" Height="0.5" Fill="{DynamicResource GoldBrush}"/>
                <Border x:Name="MiddleBorder" Background="{DynamicResource DarkBlueBrush}" Padding="12,18">
                    <ContentPresenter Content="{Binding Content, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                </Border>
            </DockPanel>
        </Border>
    </Border>

</UserControl>
