<UserControl x:Class="CSuiteViewWPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Archived: Theme switching removed (Royal only) -->
    <StackPanel>
        <TextBlock Text="Settings" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>
        <StackPanel Orientation="Horizontal">
            <RadioButton Content="Light" GroupName="Theme" IsChecked="True"/>
            <RadioButton Content="Dark" GroupName="Theme" Margin="12,0,0,0"/>
        </StackPanel>
    </StackPanel>
</UserControl>
