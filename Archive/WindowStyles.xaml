<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Shared Window Style for Styled Windows -->
    <!-- This style sets common properties for all styled windows.
         Each window defines its own content structure (header, content, footer).
         This just ensures consistent window chrome settings.
    -->
    <Style x:Key="StyledWindowTemplate" TargetType="Window">
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="AllowsTransparency" Value="True"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="ResizeMode" Value="CanResizeWithGrip"/>
    </Style>

</ResourceDictionary>
